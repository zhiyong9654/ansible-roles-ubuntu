## Contains a collection of frequently used ansible roles for Ubuntu.

### Useful arguments for Ansible
#### Asking for 'become' password
```bash
ansible-playbook playbook.yml -i inventory --ask-become-pass
```
